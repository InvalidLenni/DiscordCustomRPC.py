name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Thanks for making DiscordCustomRPC.py better with the bug report! 💛'
        pr-message: 'Thanks for your contribution to the DiscordCustomRPC.py repository! ❤'
